Create shogi timer on the web. use:

- react + vite + swr
- typescript with strict typecheck
- zustand for global state

note:

- persist state in localstorage https://zustand.docs.pmnd.rs/reference/middlewares/persist
- UI is something like the attached image. touch one side and turn is change
- enable to set Initial time and Byoyomi
- show how many turn passed somewhere in the app
- single "settings" shows menu: "pause" "resume" "initialize", show confirmation when initialize
